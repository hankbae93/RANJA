import styled from 'styled-components';
import { Input, Form, FormField, FormLabel, FormLabelSpan, FormButton } from '../common/form/form.elements';

export const LoginForm = styled(Form)``;

export const LoginField = styled(FormField)``;

export const LoginLabel = styled(FormLabel)``;

export const LabelSpan = styled(FormLabelSpan)``;

export const LoginButton = styled(FormButton)``;

export const LoginInput = styled(Input)`
  flex: 1;
`;
